#!/bin/sh

if [ -f /tmp/blockip.ipset ];then

adbyby_enable=$(uci get adbyby.@adbyby[0].enable)

if [ $adbyby_enable -eq 1 ]; then
	if pidof adbyby>/dev/null; then
	  /usr/bin/adbyby.sh reload_rule
	 else
	   /usr/bin/adbyby.sh restart
  fi
fi

fi

